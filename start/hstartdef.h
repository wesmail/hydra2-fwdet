#ifndef HSTARTDEF_H
#define HSTARTDEF_H

#include "haddef.h"

const Cat_t catStart2Raw = START_OFFSET + 1;
const Cat_t catStart2Cal = START_OFFSET + 2;
const Cat_t catStart2Hit = START_OFFSET + 3;
const Cat_t catTBoxChan  = START_OFFSET + 4;


#define  START2_MAX_MODULES     10
#define  START2_MAX_COMPONENTS 128
#define  START2_MAX_SECTORS     -1

#endif /* !HSTARTDEF_H */
